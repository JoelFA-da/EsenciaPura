# ====================
# CONFIGURACIÓN GENERAL
# ====================
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# ====================
# BASE DE DATOS
# ====================
# PostgreSQL en Supabase con parámetros optimizados para Render Free Tier
# Parámetros importantes:
# - pgbouncer=true : Usa transaction pooling (REQUERIDO para puerto 6543)
# - connection_limit=5 : Máximo 5 conexiones simultáneas por instancia
# - pool_timeout=20 : Espera hasta 20s para obtener conexión del pool
# - connect_timeout=10 : Timeout de 10s para conectar a la BD
DATABASE_URL="postgresql://USER:PASS@HOST:6543/postgres?pgbouncer=true&connection_limit=5&pool_timeout=20&connect_timeout=10"

# Para desarrollo local con SQLite:
# DATABASE_URL="file:./data/esenciapura.db"

# Para producción en Render.com:
# Usar la misma URL con volumen persistente montado en /data
# DATABASE_URL="file:/data/esenciapura.db"

# ====================
# EMAIL (SMTP)
# ====================
# Gmail (requiere App Password de https://myaccount.google.com/apppasswords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASS=xxxx-xxxx-xxxx-xxxx

# Zoho Mail (alternativa):
# SMTP_HOST=smtp.zoho.com
# SMTP_USER=admin@esenciapura.com

# Email del negocio (donde llegarán notificaciones de Google Forms)
BUSINESS_EMAIL=admin@esenciapura.com

# ====================
# AUTENTICACIÓN
# ====================
# Generar secreto random: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu-secreto-super-seguro-cambiar-en-produccion

# ====================
# NOTAS PARA RENDER.COM DEPLOYMENT
# ====================
# Configurar estas variables en Render Dashboard:
# - NODE_ENV=production
# - LOG_LEVEL=info
# - DATABASE_URL=<External Database URL from Render>
# - SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS
# - BUSINESS_EMAIL
# - JWT_SECRET (generar nuevo valor)
